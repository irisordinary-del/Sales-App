# 🔄 HANDOFF NOTES — Sales-App / Route Planner
> อัปเดตล่าสุด: 2026-07-10 (session ต่อเนื่อง — ทำครบทุกข้อใน backlog แล้ว รวมข้อ 5)
> **วัตถุประสงค์ไฟล์นี้**: วางในแชทใหม่แทนการเล่าย้อนทั้งหมด — บอก Claude ว่า "อ่านไฟล์นี้แล้วทำงานต่อ"

---

## ⚠️ สิ่งสำคัญที่สุดที่ Claude (แชทใหม่) ต้องรู้ก่อนเริ่ม

1. **Sandbox เป็นของใหม่ทุกแชท** — `/home/claude/` ของแชทเก่าหายไปแล้ว ไม่สามารถอ้างอิงถึงได้
2. **ไฟล์ที่ user จะอัปโหลดมาในแชทใหม่ คือไฟล์ที่ "ผ่านการแก้ไขแล้ว" จาก session นี้** (ดาวน์โหลดไว้แล้ว) **ไม่ใช่ไฟล์ต้นฉบับเดิมจาก GitHub repo** — สำคัญมาก: **ห้ามสันนิษฐานว่าไฟล์ที่อัปมาคือของเดิมที่ยังไม่แก้ไข** ให้ตรวจสอบเนื้อหาก่อนเสมอ (เช่น เช็คว่ามี `RouteAnalysis`, `ActivityCtrl`, `HolidayException`, `clearStores` อยู่แล้วหรือยัง) ก่อนจะสันนิษฐานว่ายังไม่มี
3. **งานทุกข้อใน backlog (1-5) เสร็จหมดแล้วใน session นี้** — **ยังไม่ได้อัปขึ้น GitHub เลยสักไฟล์** (user ยังไม่ได้กลับไปทำ) ดังนั้นของจริงบนเว็บ (Vercel) ยังเป็นเวอร์ชันเก่าอยู่ครบ — งานหลักของแชทใหม่ (ถ้ายังไม่มี feature ใหม่มาสั่ง) น่าจะเป็นเรื่อง **deploy + ทดสอบ** มากกว่า build เพิ่ม
4. **ยังไม่เคยรันจริงในเบราว์เซอร์กับข้อมูลจริงเลยสักครั้ง** ทุกอย่างผ่านแค่ `node --check` (syntax check) เท่านั้น — แนะนำให้ทดสอบจริงก่อนพรีเซนหรือใช้งานจริง โดยเฉพาะฟีเจอร์ใหญ่ๆ (Route Analysis, โหมดที่ 4 + exception วันหยุด)
5. **กฎเดิมของโปรเจกต์ยังใช้อยู่**: ทำงานใน `/home/claude/` เท่านั้น ไม่ใช้ `/mnt/user-data/uploads/` โดยตรง, ตรวจ `node --check` ทุกไฟล์ .js ก่อนส่งมอบเสมอ (index.html เช็คด้วยการแยก inline `<script>` ออกมา check ทีละบล็อก)

---

## 📁 โครงสร้างโปรเจกต์ (สรุปสั้น)
- Vanilla JS + Firebase Firestore + Leaflet + Tailwind + Vercel PWA
- 3 roles: Admin, Supervisor/ASM, Sales
- GitHub: `irisordinary-del/Sales-App` (branch: main) — Vercel auto-deploy
- Firestore หลัก: `appData/{centerId}_main/plans/{YYYY_MM}/routes/{routeId}` (แผนรายเดือน), `sellout/{centerId}_{YYYY_MM}/chunks/` (ยอดขาย รองรับ fallback format เก่า `{YYYY_MM}` ด้วย), `skuDistribution/{id}` (campaigns)

---

## ✅ งานที่ทำเสร็จแล้วทั้งหมด (เรียงตามลำดับที่ทำ)

### 1. Bug Audit เต็มโปรเจกต์
- ตรวจ syntax ทุกไฟล์ .js — ผ่านหมด
- ลบไฟล์ orphaned ที่ไม่ได้ถูกโหลดจริง: `sales-app_2.js`, `center-select.js`, `FEATURES_IMPLEMENTATION.js`, `firebase-chunks.js`
- **ยังค้าง**: ต้องลบ 4 ไฟล์นี้ออกจาก GitHub repo ด้วย (ตอนนี้ลบแค่ใน working dir)
- แก้ `sw.js` เอา reference ไฟล์ที่ลบออกจาก `STATIC_ASSETS` precache list

### 2. บั๊ก sellout dual-format (ไม่รองรับ `{centerId}_{YYYY_MM}`)
พบและแก้ในหลายจุดที่ query `sellout` โดยไม่รองรับ format ใหม่ (มี centerId prefix): `dashboard.js` (`_ensureSkuOptions`), `sku-distribution.js` (`_loadProdOptions`, `calc()`) — ทั้งหมดแก้ให้ลอง candidate ทั้ง 2 format

### 3. Performance — โหลดหลายเดือนแบบ parallel แทน sequential
เปลี่ยน `for...await` เป็น `Promise.all` ใน 6+ จุดทั่ว `sales-app.js`, `dashboard.js`, `sku-distribution.js` — เพิ่ม in-flight dedup ใน `dashboard.js` ด้วย

### 4. ฟีเจอร์ใหม่: SKU กระจายสินค้า โหมด "ระบุร้านเอง" (`sku-distribution.js`)
- Campaign เลือกขอบเขตได้ 2 แบบ: 🛣️ ตามสายวิ่ง (เดิม) / 📋 ระบุร้านเอง (ใหม่ — อัปโหลด Excel คอลัมน์ custCode)
- โหมดใหม่แสดงผลเป็นตารางยอดขายรายร้าน (ไม่ใช่ % coverage ระดับสาย)

### 5. ฟีเจอร์ใหม่: เมนู "🎉 กิจกรรม" ในหน้า Sales (`sales.html` + `sales-app.js`)
- Bottom nav เพิ่มปุ่มที่ 4 — โชว์เฉพาะ Campaign โหมด "ระบุร้านเอง" **ล็อคตามสายของ sale คนที่ login**
- กดเข้ากิจกรรม → เห็นรายชื่อร้านที่เข้าร่วม + เช็คยอดขายอัตโนมัติ
- แก้บั๊ก `.nav-item` vs `.bnav-item` class mismatch ใน `switchTab()`

### 6. หน้าสายวิ่ง — เอา Campaign โหมด custom ออก กันซ้ำซ้อนกับหน้ากิจกรรม
`_loadCampaignIcons` โชว์เฉพาะ Campaign โหมด "ตามสาย" เท่านั้น

### 7. UX Audit หน้าจัดสาย (index.html Tab 1-4) + แก้ไข
- **บั๊กจริง**: ปุ่ม "ล้างสายนี้" เรียก `App.clearStores()` ที่ไม่เคยมีอยู่จริง → สร้างฟังก์ชันจริงแล้ว + เปลี่ยนชื่อปุ่มเป็น "ลบร้านทั้งหมด"
- เปลี่ยนชื่อ Tab 3 → "แก้วันที่จัด", เพิ่ม hint 2 วิธีจัดสายบน Tab 2
- เพิ่ม: ป้ายนับร้านที่เลือก (live), legend สีของ Day, empty state ทุก tab, **Undo 1 ชั้น** (8 วิ)

### 8. Calendar Config ปรับปรุง UX
- เปลี่ยนชื่อโหมดให้บอกผลลัพธ์ตรงๆ + ตัวอย่าง + **Live preview** 7 วันแรก
- Badge โหมดปัจจุบันติดหน้าหลัก, เตือนศูนย์ใหม่ที่ยังไม่ตั้งค่า, เตือนก่อนเปลี่ยนโหมดกลางคัน

### 9. ฟีเจอร์ใหม่: หน้า "📈 วิเคราะห์สายวิ่ง" (Route Analysis)
- **หน้า 1**: ตาราง สาย × เดือน (default 6 เดือน ปรับ 3/6/12 ได้) — ยอดขาย + จำนวนร้าน**ตามแผนจริงของเดือนนั้น**
- **หน้า 2**: คลิก cell → ตาราง Day (1-24) × ทุกสาย ของเดือนนั้น พร้อมสีเขียว/เหลือง/แดงบอก% ร้านที่มียอด

### 10. ✅ โหมดที่ 4 "ตามวันในสัปดาห์" + ระบบจัดการวันหยุด (เสร็จล่าสุด)
- **โหมดที่ 4** ใน Calendar Config: Day N = วันในสัปดาห์ (เช่น Day 1 = จันทร์) ขยายทุกสัปดาห์อัตโนมัติ ไม่ต้องกรอกซ้ำทุกเดือน
- **ระบบจัดการวันหยุด** — ปุ่ม "📆 จัดการวันหยุด" หน้าหลัก: เลือกวันที่ → auto แบ่งครึ่งร้านตาม seq เดิม → **มินิแผนที่แยกต่อสาย** (คลิกจุดสลับกลุ่มได้) → ยืนยันบันทึก
- Exception ผูกกับวันที่เจาะจงเท่านั้น (ไม่แก้กฎหลัก) → รอบถัดไปที่ไม่มีวันหยุดกลับเป็นปกติเอง
- Sales app (`Processor.routeList()`) เช็ค exception ของวันนี้อัตโนมัติ ปรับ queue ให้ พร้อมข้อความ "⚠️ ปรับเนื่องจากวันหยุด" ให้ sale เห็น

**ข้อจำกัดที่รู้อยู่แล้ว (ไม่ใช่บั๊ก แต่ตั้งใจไม่ทำ)**: ฟังก์ชัน `getDateFromDay()` (reverse-map Day→วันที่) ไม่รองรับโหมด weekday เพราะ 1 Day ในโหมดนี้ตรงกับหลายวันที่ต่อเดือน (ไม่มีคำตอบเดียวที่ถูกต้อง) — ถ้ามีจุดไหนในระบบเรียกใช้ฟังก์ชันนี้กับโหมด weekday จะได้ `null` กลับมา ควรเช็คจุดที่เรียกใช้ก่อนเปิดใช้งานโหมดนี้จริงจัง

---

## 📦 ไฟล์ทั้งหมดที่แก้ไขใน session นี้ (ต้องอัปทับ GitHub ทั้งหมด)
1. `sw.js`
2. `dashboard.js`
3. `sku-distribution.js`
4. `sales-app.js`
5. `sales.html`
6. `admin-data.js`
7. `admin-ui.js`
8. `index.html`
9. `WORKING_FILES_REGISTRY.md` (ไม่บังคับ แต่แนะนำ)

**ต้องลบออกจาก GitHub ด้วย**: `sales-app_2.js`, `center-select.js`, `FEATURES_IMPLEMENTATION.js`, `firebase-chunks.js`

---

## 🔴 งานค้าง — ไม่มีแล้วในแง่ฟีเจอร์ที่วางแผนไว้ (backlog 1-5 เสร็จหมด)

สิ่งที่ควรทำต่อในแชทใหม่ (เรียงตามความสำคัญ):

1. **Deploy ขึ้น GitHub จริง** — อัปทับ 8 ไฟล์ + ลบ 4 ไฟล์ orphaned ตามลิสต์ด้านบน
2. **ทดสอบจริงในเบราว์เซอร์** อย่างน้อยฟีเจอร์หลักที่จะใช้/พรีเซน — โดยเฉพาะ:
   - โหมดที่ 4 + จัดการวันหยุด (ซับซ้อนสุด ยังไม่เคยรันจริงเลย)
   - Route Analysis (ต้องมีข้อมูล plan+ยอดขายจริงหลายเดือนถึงจะเห็นผลชัด)
   - เมนู "กิจกรรม" ในหน้า Sales (ต้องมี Campaign โหมด "ระบุร้านเอง" อยู่จริงก่อนถึงจะเห็นอะไร)
3. **Publish firestore.rules** ถ้ามีการแก้ (เช็ครอบนี้ไม่ได้แตะ rules เลย ไม่ต้อง publish ใหม่)
4. รอ feature request ใหม่จาก user

---

## 💬 วิธีเริ่มแชทใหม่
บอก Claude ว่า:
> "อ่านไฟล์ `HANDOFF_SESSION_2026-07-10_v2.md` ที่แนบมา แล้วช่วย [ระบุสิ่งที่ต้องการ เช่น ทดสอบ / deploy / ทำฟีเจอร์ใหม่]"

พร้อมแนบไฟล์ที่แก้ไขแล้วทั้งหมด (list ด้านบนหัวข้อ "ไฟล์ทั้งหมดที่แก้ไข") — **ไม่ใช่ไฟล์ต้นฉบับเดิมจาก repo**
