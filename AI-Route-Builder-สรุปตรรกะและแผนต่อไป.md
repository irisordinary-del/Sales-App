# AI Route Builder — สรุปตรรกะทั้งหมด และแผนที่คาดว่าจะทำต่อ

เอกสารนี้รวบรวมตรรกะทั้งหมดที่คุยและสร้างกันมาในเซสชันนี้ (ตั้งแต่ตรวจบัคทั้งระบบ จนถึงการปรับปรุง AI Route Builder) พร้อมวิเคราะห์ว่าขั้นต่อไปที่น่าจะต้องทำคืออะไร — ใช้เป็นจุดอ้างอิงเดียวแทนการไล่อ่าน chat ย้อนหลัง

**อัปเดตล่าสุด:** 2026-09-05

---

## 1. โครงสร้างระบบ 4 ระดับ (ตามลำดับที่ข้อมูลไหลผ่าน)

```
ศูนย์ (Center)                       เช่น "402"
  └─ สาย/เซลล์ (Route / salesCode)    เช่น "402V07" — 🔴 ระดับนี้ "ยังไม่มี AI" (ดูข้อ 7)
       └─ วัน (Day)                   เช่น "Day 1".."Day 24" — ✅ AI Route Builder จัดระดับนี้
            └─ ลำดับการเยี่ยม (Seq)   เช่น ร้านไหนไปก่อน-หลังในวันเดียวกัน — ✅ Auto-Sequence จัดระดับนี้ (ใหม่: ต่อจาก AI อัตโนมัติแล้ว)
```

ตอนนี้ระบบมี AI ช่วยจัดการ **2 ใน 3 ระดับล่าง** (วัน + ลำดับ) ส่วนระดับบนสุด (แบ่งพื้นที่ทั้งศูนย์เป็นกี่เซลล์) ยังเป็นงานมือ 100% — ดูข้อ 7

---

## 2. AI Route Builder v5 — ตรรกะทั้งหมด (`admin-ai.js`)

### 2.1 กฎธุรกิจตายตัว
- **18-30 ร้าน/วัน** (`AI.MIN_PER_DAY` / `AI.MAX_PER_DAY`) — ห้ามหลุดขอบเขตนี้เด็ดขาด ไม่ใช่แค่ "พยายามให้ใกล้เคียง"

### 2.2 Pipeline เต็ม (เรียงตามลำดับที่รันจริง)

| # | ขั้นตอน | ทำอะไร | ใช้ API จริงไหม |
|---|---|---|---|
| 0 | ดึงความสูง | ยิง `/api/elevation` ครั้งเดียวสำหรับร้านที่ยังไม่มี `s.elevation` แคชไว้ที่ร้านเลย (ครั้งหน้าไม่ยิงซ้ำ) | ✅ 1 request |
| 1 | K-Means++ clustering | จัดกลุ่มร้าน 3 รอบ เลือกผล WCSS ต่ำสุด ใช้ระยะแบบ elevation-aware (ดู 2.3) ไม่ใช่เส้นตรงล้วน | ❌ |
| 2 | จัดวันตามมุมองศา + จับคู่ freq=2 | เรียงกลุ่มรอบจุดศูนย์กลางเป็นวงกลม (กันวันที่ 1 กับวันสุดท้ายอยู่ไกลกันเกิน) จับคู่ร้าน freq=2 (ไปเดือนละ 2 ครั้ง) ให้อยู่คนละครึ่งของรอบ | ❌ |
| 3 | **Pass 1: Geo-Aware Balance** | ย้ายร้าน "borderline" (อยู่ใกล้วันข้างเคียง ≤1.4×ระยะเดิม) จากวันที่แน่นไปวันที่โล่ง | ❌ |
| 4 | **Pass 2: Fix Jumpers** | Voronoi reassignment — ส่งร้านที่อยู่ผิด "เขตอิทธิพล" กลับที่ถูก รันจนลู่เข้า (converge) หรือครบ 5 รอบ | ❌ |
| 5 | **Pass 3: Road-Verify** | ร้านที่ยัง "ดูโดด" (ใกล้วันอื่น <80% ของระยะวันตัวเอง) → เช็คด้วยเส้นทางถนนจริงผ่าน `/api/optimize-route` (ยิงเฉพาะร้านที่ต้องสงสัย ไม่ใช่ทุกร้าน) | ✅ 1 request/ร้านที่ต้องสงสัย |
| 6 | **Pass 4: Enforce Min/Max** | บังคับ 18-30 ร้าน/วันจริง — ย้ายร้านที่ใกล้ปลายทางที่สุดจนกว่าทุกวันจะอยู่ในช่วง | ❌ |
| 7 | **Pass 5: Auto-Sequence** ⭐ใหม่ | จัดลำดับการเยี่ยม **ทุกวัน** อัตโนมัติต่อเลย (เหมือนกดปุ่ม "🧭 จัดลำดับการเยี่ยมอัตโนมัติ" เองทีละวัน) เขียนผลลง `s.seqs[day]` | ✅ 1 request/วัน (สูงสุด 48 ร้าน/request) |

### 2.3 ระยะทางแบบ elevation-aware (`AI._getDist`)
ใช้แทนระยะเส้นตรงล้วนทุกจุดในอัลกอริทึม (K-means, balance, jumper-fix, enforce-minmax):
- ถ้าความสูงต่างกัน ≤100 ม. → ไม่ปรับ (ถือว่าไม่มีนัยสำคัญ)
- ถ้าต่างกันมากกว่านั้น → คูณ factor `1 + (elevDiff-100)/100` **เพดานสูงสุด 4 เท่า** (ไม่ตัดออกจากกลุ่มเด็ดขาด เพราะอาจมีถนนอ้อมภูเขาจริงที่ไม่ไกลมาก)
- ถ้าร้านไหนไม่มีข้อมูลความสูง (fetch ไม่สำเร็จ) → fallback เป็นเส้นตรงเดิมทันที ไม่ทำให้ทั้งฟีเจอร์พัง

**เหตุผลที่ออกแบบเป็น "soft penalty" ไม่ใช่ hard block:** ทดสอบกับข้อมูลจริง 402V07 (พื้นที่มีภูเขาคั่น) พบว่าความสูงต่างกันเยอะไม่ได้แปลว่าถนนอ้อมไกลเสมอไป — ถ้า block เด็ดขาดจะเกิด false positive สูง

### 2.4 Road-Verify ใช้ optimize-route API อย่างไร
ไม่ได้ขอ "ระยะทาง" ตรงๆ แต่ใช้ field `order` ของ VROOM (2 จุดปลายทางที่เป็นไปได้ → ให้ ORS บอกว่าจุดไหนควรไปก่อนจากตำแหน่งร้าน) เป็น proxy ราคาถูกว่า "วันไหนใกล้กว่าจริงตามถนน" — ยิงเฉพาะร้านที่ elevation-heuristic ชี้ว่าน่าสงสัยเท่านั้น ไม่ใช่ทุกร้าน (ประหยัดโควตา)

### 2.5 Auto-Sequence (Pass 5) — ทำไมเพิ่งเพิ่ม
**ปัญหาที่พบ:** ทดสอบ gen แผนจริงบน 402V07/มกราคม 2570 แล้วพบว่า `seqs: {}` ว่างเปล่า 100% ทุกร้าน — แปลว่า AI เดิมตัดสินใจแค่ "ร้านไหนอยู่วันไหน" (clustering) แต่ไม่เคยกำหนด "ลำดับวิ่งภายในวัน" เลย ต่อให้กลุ่มก้อนแน่นแค่ไหน ถ้าไม่มีลำดับ พนักงานขายอาจถูกปล่อยให้วิ่งซิกแซกในพื้นที่นั้นทั้งวัน

**ทางแก้:** เดิมมีปุ่ม "🧭 จัดลำดับการเยี่ยมอัตโนมัติ" (`App.optimizeDayOrder`) อยู่แล้ว แต่ต้องกดเองทีละวัน (24 ครั้งสำหรับ 402V07) — เพิ่ม Pass 5 เรียกกลไกเดียวกันนี้วนครบทุกวันอัตโนมัติ **ต่อจาก AI clustering เลย** เพื่อให้ "งานจบที่ AI" ตามที่ขอ

**เงื่อนไข:** ต้องตั้ง "จุดเริ่มต้นของสาย" ไว้ก่อน (ปุ่ม 📍 จุดเริ่ม-จุดจบ → จากศูนย์ / จากที่พัก) ถ้ายังไม่ตั้ง Pass 5 จะข้ามไปทั้งหมด (ไม่ทำให้ AI ล้มเหลว) แล้วแจ้งในข้อความสรุปตอนจบว่ายังไม่ได้จัดลำดับ พร้อมบอกให้ไปกดปุ่มไหน

**ทดสอบแล้ว:** รันบน 402V07 (531 ร้าน/24 วัน) จริง ได้ผล 24/24 วัน 100% (ทดสอบด้วย mock API ในเครื่อง ยังไม่ใช่ข้อมูลถนนจริง — ต้อง deploy ก่อนถึงจะได้ลำดับจริง)

### 2.6 แถบ % ความคืบหน้า ⭐ใหม่
เพิ่ม `UI.setLoaderProgress(pct, sub)` ใน `admin-ui.js` + progress bar ใน `index.html` (`#loader-progress-wrap`) — แสดง % จริงตลอด pipeline: ดึงความสูง (0-10%) → clustering (10-18%) → balance/jumper-fix (18-26%) → road-verify (26-42%, ไล่ตามจำนวนร้านที่ตรวจจริง) → enforce min/max (42-48%) → auto-sequence (50-98%, ไล่ตามวันที่ X/24) → เสร็จ (100%)

---

## 3. โควต้า OpenRouteService (ORS) — ตัวเลขจริงที่ยืนยันแล้ว

⚠️ **README เดิม (`จัดลำดับอัตโนมัติ-README.md`) เขียนว่า "8,000+ requests/วัน" ซึ่งผิด** — ตรวจสอบจากหน้าโควต้าทางการ (`https://openrouteservice.org/restrictions/`) แล้วพบว่า:

| Service | โควต้าจริง | Max ต่อ request |
|---|---|---|
| Optimization (ใช้ทำ road-verify + auto-sequence) | **500/วัน** + 40/นาที | 50 routes/request (โค้ดจำกัดเองไว้ที่ 48 ร้าน/วัน) |
| Matrix | 500/วัน + 40/นาที | 3,500 locations (เช่น 50×50) |
| Elevation | 2,000/วัน + 40/นาที | 2,000 vertices/request |

**การใช้งานจริงต่อการกด AI 1 ครั้ง (สาย 24 วัน แบบ 402V07):**
- Elevation: 1 request (แคชไว้ ครั้งต่อไปไม่ซ้ำถ้าร้านเดิม)
- Road-verify: ~5-10 requests (เฉพาะร้านที่ต้องสงสัยจริง)
- Auto-sequence: 24 requests (1/วัน)
- **รวม ~30-35 requests/ครั้ง** — ยังห่างจาก 500/วันมาก แต่ถ้าหลายสายในศูนย์เดียวกัน gen พร้อมกันหลายรอบวันเดียว ควรเริ่มติดตามจำนวนจริง

---

## 4. งานอื่นที่แก้ไปในเซสชันนี้ (สรุปย่อ)

- **ลบ native `confirm()` ทั้งหมด** → เปลี่ยนเป็น `UI.showConfirm()` (custom modal) เพราะ `confirm()` เบราว์เซอร์ใช้ไม่ได้จริงในระบบนี้ — แก้ไปแล้ว 6 จุดใน `admin-data.js`, `users.js`, `tasks.js`, `index.html`
- **AuditLog** — แก้ centerId fallback, แก้ `write()` ให้ await จริง, เพิ่ม hook ที่ login/logout/create/update/delete user, เพิ่มสคริปต์ `audit-log.js` ในทุกหน้า (ไม่ใช่แค่ index.html)
- **`App.clearAllAssignments()` / `App.clearStores()`** — แก้บั๊กกดเคลียร์แล้วไม่ทำงาน (ต้นเหตุคือ `confirm()` เดียวกัน)
- **`api/optimize-route.js`** — แก้ "Invalid profile: car." (ลืมใส่ `profile: 'driving-car'`) + เพิ่ม `distanceKm`/`durationMin` ในผลลัพธ์
- **ลบขั้นตอน "ตั้งเป็นเดือนที่ใช้งานจริง"** — ฝั่งเซล/supervisor สลับเดือนอัตโนมัติตามวันที่จริงของเครื่อง ไม่ต้องกดเผยแพร่มือแล้ว
- **รีดีไซน์ toolbar** — รวมปุ่ม +เดือน/+สาย เข้าไอคอน ⚙️, ลบ badge "LIVE", ลบฟีเจอร์ "จัดการวันหยุด" ที่ตายแล้ว, ปรับสีปุ่มให้เป็นชุดเดียว
- **สร้างเดือนมกราคม 2570 จริงบน production** ให้เข้าไปดูผล AI ได้เอง (402V07, 531 ร้าน)

---

## 5. สถานะ Deploy (สำคัญ — ยังไม่ได้ขึ้น production)

ไฟล์ที่แก้/สร้างใหม่ทั้งหมดยัง**อยู่ในเครื่องเท่านั้น** ต้อง push ขึ้น GitHub ให้ Vercel auto-deploy (หรือ deploy มือ):

| ไฟล์ | สถานะ |
|---|---|
| `api/elevation.js` | 🆕 ใหม่ |
| `admin-ai.js` | ✏️ v5 (elevation + road-verify + min/max + auto-sequence + progress) |
| `admin-ui.js` | ✏️ เพิ่ม `setLoaderProgress` |
| `admin-data.js` | ✏️ แก้ confirm() bug |
| `users.js` / `tasks.js` | ✏️ แก้ confirm() bug |
| `index.html` | ✏️ progress bar markup + toolbar redesign |
| `api/optimize-route.js` | ✏️ แก้ profile bug + distanceKm/durationMin |

**หมายเหตุ:** การทดสอบ auto-sequence/progress bar ที่ผ่านมาทั้งหมดรันบน mock server ในเครื่อง (`localhost:5173`) — ไม่ได้ยิง ORS จริง ไม่เปลืองโควต้าจริงแม้แต่ request เดียว ต้อง deploy ก่อนถึงจะได้ผลลัพธ์จากถนน/ความสูงจริง

---

## 6. Testing Methodology ที่ใช้ตลอดเซสชัน

1. ทดสอบบนสำเนาข้อมูลจริง (ไม่ใช่ข้อมูลปลอม) แต่แยก route/collection ต่างหาก หรือใช้ prefix `TEST_` เมื่อไรก็ตามที่จะกระทบข้อมูลจริง
2. ยืนยันผลด้วยการอ่าน Firestore ตรงๆ (ไม่เชื่อแค่ toast message)
3. ล้างข้อมูลทดสอบทุกครั้งหลังใช้เสร็จ (ลบ doc + เอาออกจาก `routeList`) แล้ว verify ซ้ำ
4. รัน `node --check` ทุกไฟล์ JS ก่อนถือว่าแก้เสร็จ
5. เคลียร์ Service Worker + Cache ก่อนเทสทุกรอบใน Browser pane (กันปัญหาโค้ดเก่าค้าง)

---

## 7. 🆕 หัวข้อที่กำลังคุยกันอยู่ — แบ่งพื้นที่เป็นเซลล์ (ยังไม่ได้เขียนโค้ด)

### โจทย์
ตอนนี้ AI ทำงานได้แค่ระดับ "มีร้านที่กำหนดเซลล์ (สาย) แล้ว → จัดวัน" เท่านั้น ยังไม่มีระดับ **"มีแต่พื้นที่ทั้งศูนย์ที่ยังไม่กำหนดเซลล์เลย → แบ่งเป็น K เซลล์"**

ตัวอย่างที่ยกมา: 1 พื้นที่ อยากได้ 7 เซลล์ จากร้านค้าทั้งหมดในศูนย์

### แนวทางที่คุยกันไว้
- ใช้อัลกอริทึมชุดเดิม (K-Means++ + elevation-aware + jumper-fix) ได้เลย แค่เปลี่ยนความหมายของ K จาก "จำนวนวัน" เป็น "จำนวนเซลล์" — ไม่ต้องเขียนคณิตศาสตร์ใหม่
- **จุดต่างสำคัญ:** เกณฑ์สมดุลระดับเซลล์ต้องคิดเป็น "ภาระงานทั้งเดือน" ไม่ใช่แค่จำนวนร้านเท่าๆ กัน เพราะ:
  - ร้าน freq=2 (ไปเดือนละ 2 ครั้ง) ต้องนับ 2 เที่ยว ไม่ใช่นับร้านเดียว
  - แต่ละเซลล์ควรได้จำนวนร้านที่ทำให้ "จัดวันได้ 18-30 ร้าน/วันจริง" ตามกฎเดิม เมื่อหารด้วยจำนวนวันทำงาน/เดือน
- **ข้อเสนอ flow:** "1. แบ่งเซลล์ (สร้างสายใหม่ K สาย) → 2. เข้าแต่ละสายไปจัดวันด้วย AI Route Builder เดิม" — เป็น 2 ขั้นตอนแยกกัน ไม่รวมเป็นปุ่มเดียว
- ระบบมีฟีเจอร์ **"Gen from Routes"** อยู่แล้วใน `users.js` (`Auth.genUsersFromRoutes`) ที่สร้าง user account ให้อัตโนมัติจาก routeList ของศูนย์ — ถ้าแบ่งเซลล์เสร็จแล้วสร้างเป็นสายใหม่ K สาย จะต่อเข้ากับฟีเจอร์นี้ได้ทันทีเพื่อสร้าง login ให้เซลล์ใหม่แต่ละคน

### ✅ การตัดสินใจที่ยืนยันแล้ว (2026-09-05)

| หัวข้อ | มติ |
|---|---|
| ขอบเขตจังหวัด (`store.province`) | **Hard boundary เด็ดขาด** ห้ามเซลล์ไหนคาบ 2 จังหวัด — **ยกเว้น**: จังหวัดที่มีร้านรวมกันไม่ถึงเกณฑ์ขั้นต่ำสำหรับตั้งเป็น 1 เซลล์ได้จริง → รวมร้านทั้งจังหวัดนั้นเข้ากับเซลล์ของจังหวัดข้างเคียงที่ใกล้ที่สุดแทน |
| ขอบเขตอำเภอ (`store.district`) | **Soft boundary** พยายามให้แต่ละเซลล์อยู่อำเภอเดียว ยอมข้ามได้บ้างถ้าจำเป็นจริง (เช่น อำเภอเล็กเกินไปจนได้ร้านไม่พอ) |
| ตั้งชื่อเซลล์ใหม่ | **แอดมินตั้งเอง** ทุกกลุ่มก่อนยืนยันสร้างเป็นสายจริง ไม่ auto-name |
| ร้านที่มีเซลล์อยู่แล้ว vs ร้านใหม่ | **รองรับทั้ง 2 โหมด**: (a) แบ่งเฉพาะร้านที่ยังไม่มีเซลล์ (ไม่แตะร้านเดิม) (b) จัดใหม่ทั้งหมดร่วมกัน (รวมร้านเดิม+ใหม่คิดใหม่ทั้งพื้นที่) |
| ลำดับความสำคัญตอนเกลี่ยสมดุล | จังหวัด (ข้อ1) > อำเภอ (ข้อ2) > สมดุลจำนวนร้าน — ตั้งเป้าส่วนต่างเซลล์ที่มาก-น้อยสุด **ไม่เกิน 50 ร้าน** แต่ยอมเกินได้ถ้าโครงสร้างจังหวัด/อำเภอจริงไม่เอื้อ (ไม่ฝืนแหก 2 ข้อบนเพื่อให้ได้ 50) |

### ตรรกะที่ยืนยันแล้ว — ลำดับขั้นตอนเต็ม

1. กลุ่มร้านตามจังหวัดก่อนเป็นอันดับแรก (hard boundary)
2. เช็คจังหวัดที่มีร้านรวมกันไม่ถึงเกณฑ์ขั้นต่ำ 1 เซลล์ → ควบรวมเข้ากับจังหวัดข้างเคียงที่ใกล้ที่สุดทางภูมิศาสตร์ก่อน (exception ของข้อ 1)
3. จัดสรรจำนวนเซลล์ K ให้แต่ละกลุ่มจังหวัด(ที่ควบรวมแล้ว)ตามสัดส่วนจำนวนร้าน — ใช้ largest-remainder method กันปัดเศษแล้วเซลล์หาย
4. ภายในแต่ละกลุ่ม แบ่งตามอำเภอต่อ (soft boundary) ก่อนเข้าขั้นตอน clustering จริง
5. ใช้ K-Means++ + elevation-aware clustering ชุดเดิม (ของ AI Route Builder) จัดร้านภายในขอบเขตที่ล็อกจากข้อ 1-4 ให้เป็น K ย่อยจริง
6. เช็คสมดุลหลังจัดเสร็จ: ส่วนต่างร้าน "มากสุด-น้อยสุด" ตั้งเป้า ≤50 ร้าน เกลี่ยได้เท่าที่ไม่ละเมิดข้อ 1 และเลี่ยงละเมิดข้อ 4 ให้มากที่สุด
7. แสดงผลลัพธ์ให้แอดมินตรวจ+ตั้งชื่อเซลล์เองทุกกลุ่ม ก่อนกดยืนยันสร้างเป็นสายจริง
8. รองรับ 2 โหมดเข้า (แบ่งเฉพาะร้านใหม่ / จัดใหม่ทั้งหมด) ตามที่เลือกตอนเริ่ม

**สถานะ:** ดีไซน์ครบแล้ว ยังไม่เริ่มเขียนโค้ด — รอคิวถัดจากงาน deploy รอบปัจจุบัน

---

## 8. 🔮 คาดการณ์สิ่งที่น่าจะอยากทำต่อ (วิเคราะห์จากทิศทางการคุยทั้งเซสชัน)

เรียงตามลำดับที่คาดว่าจะเป็นความต้องการถัดไป พร้อมเหตุผล:

### 8.1 Deploy ของที่ค้างอยู่ + ทดสอบด้วย ORS จริง (ความน่าจะเป็นสูงสุด)
ทุกอย่างในข้อ 5 ยังไม่ขึ้น production เลย และการทดสอบ elevation/road-verify/auto-sequence ทั้งหมดที่ผ่านมาใช้ mock — ยังไม่เคยเห็นผลจริงจากภูเขา/ถนนจริงสักครั้ง นี่คือ blocker ของเกือบทุกงานที่เหลือ

### 8.2 ระบบดูแผนที่เส้นทางจริง (visualize เส้นวิ่งต่อวัน)
คุณเคยถามตรงๆ ว่า "ภาพที่ผมส่งไปวิ่งแบบนี้ได้จริงๆใช่ไหม" — ตอนนี้ตอบได้แค่ตัวเลข (ระยะจาก centroid, จำนวนร้าน/วัน) แต่ยังไม่มี **เส้นทางที่ลากจริงบนแผนที่ตามลำดับ seq** ให้ดูด้วยตา หลัง deploy auto-sequence แล้ว น่าจะอยากเห็นเส้น polyline ของแต่ละวันวาดบนแผนที่ (ใช้ `geometry` ที่ ORS Optimization API ส่งกลับมาอยู่แล้วแต่ยังไม่ได้ใช้ — ตอนนี้ codebase ใช้แค่ `order`)

### 8.3 ฟีเจอร์แบ่งเซลล์ (ข้อ 7) — ต่อยอดจากที่เพิ่งคุย
เมื่อเคลียร์คำถาม 4 ข้อในหัวข้อ 7 แล้ว น่าจะขอให้เริ่มออกแบบ/เขียนจริง เพราะเป็นช่องว่างสุดท้ายของ pipeline (จากพื้นที่ดิบ → ถึงลำดับการเยี่ยม)

### 8.4 Sanity-check เชิงระยะทาง/เวลาต่อวัน ก่อน publish
ตอนนี้ `distanceKm`/`durationMin` ที่เพิ่งเพิ่มใน `api/optimize-route.js` (ยังไม่ deploy) ยังไม่ถูกใช้แสดงผลที่ไหนเลย — น่าจะอยากเห็นเป็น badge เตือนถ้าวันไหนระยะ/เวลารวมเกินเกณฑ์ที่สมเหตุสมผล (เช่น ขับเกิน 6-8 ชม./วัน) ก่อนที่จะปล่อยให้เซลล์ใช้จริง

### 8.5 Quota monitoring / safeguard
คุณถามเรื่องโควต้าไปแล้ว 2 รอบ (ก่อนสร้างเดือนมกราคม 2570 และหลังทดสอบรอบนี้) — แปลว่าเป็นเรื่องที่กังวลจริง โดยเฉพาะถ้าหลายสายในศูนย์ gen พร้อมกัน อาจอยากได้ตัวนับ/แจ้งเตือนก่อนเกินโควต้า 500/วัน แทนที่จะให้ผมนับมือให้ทุกครั้ง

### 8.6 ทดสอบ role อื่น (sales/supervisor) กับแผนที่ auto-sequence สร้างให้
Auto-sequence ใหม่นี้ยังทดสอบแค่ฝั่งแอดมิน — ฝั่งเซลที่จะเห็นลำดับการเยี่ยมจริง (sales.html) ยังไม่ได้ตรวจว่าแสดงผล `seqs` ที่ AI สร้างให้อัตโนมัติถูกต้องไหม (ต่างจากกรณีที่แอดมินกดปุ่มจัดลำดับเองทีละวัน)

---

*เอกสารนี้สรุปจากบทสนทนาในเซสชันนี้เท่านั้น ไม่ได้แทนที่การอ่านโค้ดจริงเมื่อจะแก้ไขต่อ — โครงสร้างโค้ด/ชื่อฟังก์ชันอาจเปลี่ยนได้ในอนาคต*
