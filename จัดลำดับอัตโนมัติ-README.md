# ฟีเจอร์ใหม่: จัดลำดับการเยี่ยมอัตโนมัติ (Auto-Sequence)

คำนวณลำดับการวิ่งร้านค้าในแต่ละวันให้อัตโนมัติ จากจุดเริ่มต้นของสาย → ร้านทั้งหมดในวันนั้น → (กลับจุดเดิม หรือไม่ก็ได้) โดยใช้เส้นทางถนนจริง ไม่ใช่เส้นตรง

ใช้ **OpenRouteService Optimization API** (บริการฟรี ครอบ VROOM engine ไว้ให้) เป็นผู้คำนวณ — ไม่ต้องเช่าเซิร์ฟเวอร์เอง ไม่มีค่าใช้จ่าย

---

## ไฟล์ที่เปลี่ยนแปลง

| ไฟล์ | สถานะ | สิ่งที่เปลี่ยน |
|---|---|---|
| `api/optimize-route.js` | 🆕 ไฟล์ใหม่ | Vercel Serverless Function — พร็อกซีไปยัง OpenRouteService |
| `admin-data.js` | ✏️ แก้ไข | เพิ่มฟังก์ชัน `getRouteStartConfig`, `saveDepotLocation`, `saveRouteStartConfig`, `optimizeDayOrder` + อ่าน `depotLocation`/`routeSettings` จาก Firestore |
| `index.html` | ✏️ แก้ไข | เพิ่มปุ่ม "📍 จุดเริ่ม-จุดจบ" ในหน้าจัดสาย + Modal ตั้งค่า + ปุ่ม "🧭 จัดลำดับการเยี่ยมอัตโนมัติ" ใน Day Modal |
| `firestore.rules` | ✏️ แก้ไข | อนุญาต field ใหม่ `depotLocation`/`routeSettings` บน document ศูนย์ (`appData/{center}_main`) |

**นำไปใช้:** แทนที่ไฟล์ 3 ไฟล์แรกในโปรเจกต์เดิม แล้ว deploy `firestore.rules` ใหม่ผ่าน Firebase Console หรือ `firebase deploy --only firestore:rules`

---

## ขั้นตอนติดตั้ง (ทำครั้งเดียว)

### 1. สมัคร API Key ฟรีจาก OpenRouteService
1. ไปที่ https://openrouteservice.org/dev/#/signup สมัครบัญชีฟรี
2. หลังยืนยันอีเมล เข้า Dashboard → กด "Request a token" เลือก **Standard** (ฟรี)
3. คัดลอก API Key ที่ได้

โควตาฟรี: มากกว่า 8,000 คำขอ/วัน (รวมทุก endpoint ของ ORS) — เพียงพอสำหรับใช้งานจริงมาก โดยไม่มีค่าใช้จ่าย

### 2. ตั้งค่า Environment Variable บน Vercel
1. เข้า Vercel Dashboard → เลือกโปรเจกต์ → **Settings → Environment Variables**
2. เพิ่มตัวแปรใหม่:
   - Name: `ORS_API_KEY`
   - Value: (API Key จากขั้นตอนที่ 1)
   - Environment: เลือกทั้ง Production/Preview/Development
3. กด Save แล้ว **Redeploy** โปรเจกต์อีกครั้ง (ตัวแปรใหม่จะมีผลตอนถัด deploy เท่านั้น)

### 3. อัปเดต Firestore Security Rules
Deploy ไฟล์ `firestore.rules` ที่แก้ไขแล้วไปแทนที่ของเดิม (ผ่าน Firebase Console → Firestore → Rules → วางทับ หรือ `firebase deploy --only firestore:rules`)

---

## วิธีใช้งาน (ฝั่งแอดมิน)

1. เข้าหน้า **📍 วางแผนคิวงาน** เลือกสายที่ต้องการ
2. กดปุ่ม **📍 จุดเริ่ม-จุดจบ** ที่แถบด้านบน
   - ตั้งพิกัดศูนย์ (ใช้ร่วมกันทุกสาย) — ถ้าจะใช้โหมด "จากศูนย์"
   - เลือกโหมดของสายนี้: 🏢 จากศูนย์ / 🏠 จากที่พัก / 🚫 ไม่มี
   - ถ้าเลือก "จากที่พัก" ให้ปักพิกัดที่พักของสายนี้เพิ่ม
   - ติ๊ก/ปลดติ๊ก "กลับจุดเดิมหลังเยี่ยมร้านสุดท้าย" ตามต้องการ
   - กด "💾 บันทึกของสายนี้"
3. จัดร้านค้าลงวันตามปกติ (แท็บ "2. จัดสาย" หรือ "3. แก้วันที่จัด")
4. เปิดดูสรุปรายวัน (แท็บ "4. สรุป" หรือ่านที่การ์ดวันในหน้าแดชบอร์ด) แล้วคลิกวันที่ต้องการ
5. ใน Modal ที่เปิดขึ้น กดปุ่ม **🧭 จัดลำดับการเยี่ยมอัตโนมัติ** ด้านล่าง
   - ระบบจะคำนวณเส้นทางจริงผ่าน OpenRouteService แล้วเรียงลำดับร้าน (`seqs`) ให้อัตโนมัติ
   - ผลลัพธ์บันทึกเหมือนการลากเรียงเอง — **แก้ไขด้วยมือภายหลังได้ตามปกติ** (ในแอปฝั่งเซล ผ่านการลาก)

---

## ข้อจำกัดที่ควรรู้

- **สูงสุด 48 ร้าน/วัน** ต่อการจัดลำดับ 1 ครั้ง (ข้อจำกัดของแผนฟรี OpenRouteService ที่ ~50 locations/request — เผื่อที่ไว้ให้จุดเริ่ม/จุดจบ) ถ้าวันไหนเกิน ระบบจะแจ้งเตือนและไม่คำนวณให้ ต้องแบ่งร้านหรือจัดบางส่วนด้วยมือ
- ต้องมีพิกัด (lat/lng) ครบทุกร้านที่จะจัดลำดับ — ถ้าร้านไหนไม่มีพิกัด ระบบจะแจ้งเตือนก่อนเริ่มคำนวณ
- ถ้าบางร้านคำนวณเส้นทางไม่ได้ (พิกัดผิดปกติ/อยู่ไกลเกินไปจากถนน) ระบบจะจัดลำดับเท่าที่ทำได้ และแจ้งเตือนว่ามีกี่ร้านที่ต้องเรียงเพิ่มด้วยมือ
- โหมด "ไม่มี" (จุดเริ่มต้น) = ปิดการใช้ฟีเจอร์นี้สำหรับสายนั้น (ยังจัดลำดับด้วยมือแบบลากได้ตามปกติ)

---

## สถาปัตยกรรม (สำหรับคนต่อโค้ด)

```
[ปุ่มใน Day Modal] → App.optimizeDayOrder(day)
    → ตรวจสอบเงื่อนไข (จำนวนร้าน, พิกัด, ตั้งค่าจุดเริ่มต้นแล้วหรือยัง)
    → POST /api/optimize-route  { start, end, jobs:[{id,lat,lng}] }
        → api/optimize-route.js (Vercel Serverless Function)
            → แปลง id ร้านค้า (string) เป็นเลขลำดับชั่วคราว (VROOM ต้องการ integer id)
            → POST https://api.openrouteservice.org/optimization
              (ใช้ ORS_API_KEY จาก Environment Variable — ไม่หลุดไปฝั่ง client)
            → แปลงผลลัพธ์กลับเป็น id ร้านค้าเดิม
        ← { order: [storeId, ...] }
    → เขียน s.seqs[day] = ลำดับใหม่ ให้ทุกร้าน
    → App.saveDB()  (บันทึกเข้า Firestore เหมือนการแก้ไขปกติ)
```

`depotLocation` (พิกัดศูนย์ ใช้ร่วมกันทุกสาย) และ `routeSettings` (ตั้งค่าต่อสาย: โหมด/พิกัดที่พัก/กลับจุดเดิมหรือไม่) เก็บไว้ที่ระดับ document ของศูนย์ (`appData/{centerId}_main`) ไม่ผูกกับเดือนใดเดือนหนึ่ง — ตั้งครั้งเดียวใช้ได้ทุกเดือน
