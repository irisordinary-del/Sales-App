# 🔄 HANDOFF NOTES — Sales-App / Route Planner
> สร้างเมื่อ: 2026-07-10 (ต่อจาก session ยาวที่ token ใกล้หมด)
> **วัตถุประสงค์ไฟล์นี้**: วางในแชทใหม่แทนการเล่าย้อนทั้งหมด — บอก Claude ว่า "อ่านไฟล์นี้แล้วทำงานต่อ"

---

## ⚠️ สิ่งสำคัญที่สุดที่ Claude (แชทใหม่) ต้องรู้ก่อนเริ่ม

1. **Sandbox เป็นของใหม่ทุกแชท** — `/home/claude/` ของแชทเก่าหายไปแล้ว ไม่สามารถอ้างอิงถึงได้
2. **ไฟล์ที่ user จะอัปโหลดมาในแชทใหม่ คือไฟล์ที่ "ผ่านการแก้ไขแล้ว" จาก session นี้** (ดาวน์โหลดไว้แล้ว) **ไม่ใช่ไฟล์ต้นฉบับเดิมจาก GitHub repo** — สำคัญมาก: **ห้ามสันนิษฐานว่าไฟล์ที่อัปมาคือของเดิมที่ยังไม่แก้ไข** ให้ตรวจสอบเนื้อหาก่อนเสมอ (เช่น เช็คว่ามี `RouteAnalysis`, `ActivityCtrl`, `clearStores` อยู่แล้วหรือยัง) ก่อนจะสันนิษฐานว่ายังไม่มี
3. **ยังไม่ได้อัปขึ้น GitHub เลยสักไฟล์** ในทุกงานที่ทำใน session นี้ (user บอกว่าตอนนั้นอยู่นอกบ้าน อัปไม่ได้) — ดังนั้นของจริงบนเว็บ (Vercel) ยังเป็นเวอร์ชันเก่าอยู่ครบ
4. **กฎเดิมของโปรเจกต์ยังใช้อยู่**: ทำงานใน `/home/claude/` เท่านั้น ไม่ใช้ `/mnt/user-data/uploads/` โดยตรง, ตรวจ `node --check` ทุกไฟล์ .js ก่อนส่งมอบเสมอ

---

## 📁 โครงสร้างโปรเจกต์ (สรุปสั้น)
- Vanilla JS + Firebase Firestore + Leaflet + Tailwind + Vercel PWA
- 3 roles: Admin, Supervisor/ASM, Sales
- GitHub: `irisordinary-del/Sales-App` (branch: main) — Vercel auto-deploy
- Firestore หลัก: `appData/{centerId}_main/plans/{YYYY_MM}/routes/{routeId}` (แผนรายเดือน), `sellout/{centerId}_{YYYY_MM}/chunks/` (ยอดขาย รองรับ fallback format เก่า `{YYYY_MM}` ด้วย), `skuDistribution/{id}` (campaigns)

---

## ✅ งานที่ทำเสร็จแล้วใน session นี้ (เรียงตามลำดับที่ทำ)

### 1. Bug Audit เต็มโปรเจกต์
- ตรวจ syntax ทุกไฟล์ .js — ผ่านหมด
- ลบไฟล์ orphaned ที่ไม่ได้ถูกโหลดจริง: `sales-app_2.js`, `center-select.js`, `FEATURES_IMPLEMENTATION.js`, `firebase-chunks.js`
- **ยังค้าง**: ต้องลบ 4 ไฟล์นี้ออกจาก GitHub repo ด้วย (ตอนนี้ลบแค่ใน working dir)
- แก้ `sw.js` เอา reference ไฟล์ที่ลบออกจาก `STATIC_ASSETS` precache list

### 2. บั๊ก sellout dual-format (ไม่รองรับ `{centerId}_{YYYY_MM}`)
พบและแก้ในหลายจุดที่ query `sellout` โดยไม่รองรับ format ใหม่ (มี centerId prefix):
- `dashboard.js`: `_ensureSkuOptions` (SKU whitelist modal)
- `sku-distribution.js`: `_loadProdOptions` (autocomplete), `calc()` (คำนวณผล campaign — จุดสำคัญสุด)
- ทั้งหมดแก้ให้ลอง candidate ทั้ง 2 format (`{cid}_{ym}` ก่อน แล้ว fallback `{ym}`)

### 3. Performance — โหลดหลายเดือนแบบ parallel แทน sequential
เปลี่ยน `for...await` เป็น `Promise.all` ใน 6 จุด: `sales-app.js` (`_loadCampaignIcons`, `ActivityCtrl._checkPurchases`), `dashboard.js` (การ์ด Campaign, รายละเอียดรายวัน, `_ensureSkuOptions`), `sku-distribution.js` (`calc()`) — เพิ่ม in-flight dedup ใน `dashboard.js` (`loadMonthRows`) ด้วย

### 4. ฟีเจอร์ใหม่: SKU กระจายสินค้า โหมด "ระบุร้านเอง" (`sku-distribution.js`)
- Campaign เลือกขอบเขตได้ 2 แบบ: 🛣️ ตามสายวิ่ง (เดิม) / 📋 ระบุร้านเอง (ใหม่ — อัปโหลด Excel คอลัมน์ custCode)
- โหมดใหม่แสดงผลเป็นตารางยอดขายรายร้าน (ไม่ใช่ % coverage ระดับสาย)
- ใช้สำหรับกิจกรรมแบบ "ค่าเช่า Headboard" ที่มีลิสต์ร้านเข้าร่วมเฉพาะ

### 5. ฟีเจอร์ใหม่: เมนู "🎉 กิจกรรม" ในหน้า Sales (`sales.html` + `sales-app.js`)
- Bottom nav เพิ่มปุ่มที่ 4 — โชว์เฉพาะ Campaign โหมด "ระบุร้านเอง"
- **ล็อคตามสายของ sale คนที่ login** — เห็นแค่ร้าน/กิจกรรมที่เกี่ยวกับสายตัวเอง ไม่เห็นทั้งศูนย์ (แก้บั๊กที่เจอทีหลัง)
- กดเข้ากิจกรรม → เห็นรายชื่อร้านที่เข้าร่วม + เช็คยอดขายอัตโนมัติ ขึ้นเครื่องหมาย ✅ มียอดแล้ว / — ยังไม่มียอด
- แก้บั๊ก `.nav-item` vs `.bnav-item` class mismatch ใน `switchTab()` (ของเดิม active state ค้าง ไม่ดับตอนสลับ tab)

### 6. หน้าสายวิ่ง (route tab) — เอา Campaign โหมด custom ออก กันซ้ำซ้อนกับหน้ากิจกรรม
- `_loadCampaignIcons` โชว์เฉพาะ Campaign โหมด "ตามสาย" เท่านั้น (สินค้ากระจาย)
- Campaign โหมด "ระบุร้านเอง" ย้ายไปอยู่หน้า "กิจกรรม" ที่เดียว จบในตัว

### 7. UX Audit หน้าจัดสาย (index.html Tab 1-4) + แก้ไข
- **บั๊กจริง**: ปุ่ม "ล้างสายนี้" เรียก `App.clearStores()` ที่ไม่เคยมีอยู่จริง → สร้างฟังก์ชันจริงให้แล้ว + เปลี่ยนชื่อปุ่มเป็น "ลบร้านทั้งหมด"
- เปลี่ยนชื่อ Tab 3 "แก้ไข" → "แก้วันที่จัด" (กันสับสนว่าจะแก้ข้อมูลร้าน)
- เพิ่ม hint อธิบาย 2 วิธีจัดสาย (AI vs เลือกเอง) บน Tab 2
- เพิ่ม: ป้ายนับร้านที่เลือกอยู่ (live), legend สีของ Day บนแผนที่ (ปุ่ม "🎨 สีวัน"), empty state พร้อมคำแนะนำทุก tab, **Undo 1 ชั้น** (banner "↩️ ย้อนกลับ" 8 วิหลังกดลบ/เคลียร์)

### 8. Calendar Config (`CalendarAdmin` ใน index.html) ปรับปรุง UX
- เปลี่ยนชื่อโหมดให้บอกผลลัพธ์ตรงๆ (เช่น "วันที่ = Day ตรงกัน" แทน "วันที่จริง") + ตัวอย่าง 1 บรรทัดใต้แต่ละโหมด
- **Live preview** 7 วันแรกของเดือน อัปเดตสดตามค่าที่กรอก
- Badge โหมดปัจจุบันติดหน้าหลัก (ไม่ต้องเปิด settings ก็รู้)
- ศูนย์ใหม่ที่ยังไม่เคยตั้งค่า → เตือนแทน default เงียบๆ
- เปลี่ยนโหมดของ plan ที่มีอยู่แล้ว → เตือนแรงก่อนบันทึก (กระทบ "Day X" ข้ามเดือน)

### 9. ฟีเจอร์ใหม่: หน้า "📈 วิเคราะห์สายวิ่ง" (Route Analysis) — เมนูใหม่ทั้งหน้า (`dashboard.js` + `index.html` + `admin-ui.js`)
- **หน้า 1**: ตาราง สาย × เดือน (default 6 เดือน ปรับ 3/6/12 ได้) — ยอดขาย + จำนวนร้าน**ตามแผนจริงของเดือนนั้น** (ไม่ใช่ปัจจุบันแปะทุกเดือน)
- **หน้า 2**: คลิก cell เดือนไหนก็ได้ → ตาราง Day (1-24) × ทุกสาย ของเดือนนั้น พร้อมสีเขียว/เหลือง/แดงบอก% ร้านที่มียอด
- โหลดข้อมูลแบบ parallel ทุกเดือน

---

## 📦 ไฟล์ทั้งหมดที่แก้ไขใน session นี้ (ต้องอัปทับ GitHub ทั้งหมด)
1. `sw.js`
2. `dashboard.js`
3. `sku-distribution.js`
4. `sales-app.js`
5. `sales.html`
6. `admin-data.js`
7. `admin-ui.js`
8. `index.html`
9. `WORKING_FILES_REGISTRY.md` (ไม่บังคับ แต่แนะนำ)

**ต้องลบออกจาก GitHub ด้วย**: `sales-app_2.js`, `center-select.js`, `FEATURES_IMPLEMENTATION.js`, `firebase-chunks.js`

---

## 🔴 งานค้าง — ข้อ 5 (ยังไม่เริ่มทำ) — Spec เต็ม

### บริบท
ระบบตั้งค่าปฏิทิน (`CalendarAdmin`) ตอนนี้มี 3 โหมด: Cycle (หมุนนับต่อเนื่อง), Date (วันที่ตรง Day), Fixed (กำหนดเอง) — พบว่า**ยังไม่รองรับ**ศูนย์ที่ล็อควันตามวันในสัปดาห์ (เช่น "สาย A วิ่งเฉพาะ จันทร์ อังคาร พุธ ทุกสัปดาห์") ซึ่งพบว่า**มีหลายศูนย์ใช้แบบนี้จริง** จึงตกลงว่าควรสร้างเป็นโหมดที่ 4

### 🆕 โหมดที่ 4: "ตามวันในสัปดาห์" (Weekday Recurring)

**กฎหลัก**: Day N = วันในสัปดาห์ที่กำหนด (เช่น Day 1 = จันทร์) กำหนดครั้งเดียว ระบบขยายให้เองทุกสัปดาห์ตลอดเดือน (ไม่ต้องกรอกวันที่ซ้ำทุกเดือนแบบโหมด Fixed)
- รองรับร้านวิ่ง 1 วัน/สัปดาห์ ปกติ และร้าน **F2** (field `freq: 2` ที่มีอยู่แล้วในระบบ — วิ่ง 2 วัน/สัปดาห์)

**ระบบ Exception วันหยุด** (ส่วนที่ซับซ้อนสุด):
- ข้อยกเว้นผูกกับ**วันที่ปฏิทินเจาะจงเท่านั้น** (ไม่แก้กฎหลัก) → เดือน/สัปดาห์ถัดไปที่ไม่มีวันหยุด กลับเป็นปกติเองอัตโนมัติ
- เมื่อ admin เลือกวันที่เป็นวันหยุด → ร้านที่ปกติไปวันนั้นถูก**แบ่งครึ่งอัตโนมัติ**ไปวันก่อนหน้ากับวันถัดไป
- วิธีแบ่ง: **เรียงตาม seq เดิมของร้านในสายนั้น** (ลำดับการเดินทางที่มีอยู่แล้วในระบบ) → ครึ่งแรก (seq ต้นๆ) ไปวันก่อนหน้า / ครึ่งหลัง (seq ท้ายๆ) ไปวันถัดไป **อัตโนมัติไม่ต้องเลือกมือ**

**หน้าจอ "ตรวจสอบ/ปรับก่อนยืนยัน" (สำคัญ — ห้ามข้าม)**:
1. Admin เลือกวันที่เป็นวันหยุด → ระบบแบ่งครึ่งอัตโนมัติตาม seq
2. **เด้งแผนที่ขึ้นมาก่อนบันทึกจริง** — ร้าน 2 กลุ่มที่ถูกแบ่ง แสดงคนละสี (กลุ่มไปวันก่อนหน้า vs วันถัดไป) ซ้อนบนแผนที่เดียวกับร้านเดิมของวันนั้น
3. **Admin ลากร้านสลับกลุ่มได้** (ใช้กลไกลากวางเดิมที่มีอยู่แล้วในหน้าจัดสาย ไม่ต้องสร้างใหม่) — เพราะการแบ่งด้วย seq เป็นแค่จุดเริ่มต้นที่ระบบเดาให้ ไม่ใช่คำตอบสุดท้ายเสมอไป (ร้านลำดับติดกันอาจไม่ได้อยู่ใกล้กันจริงทางภูมิศาสตร์)
4. กด "ยืนยัน" ถึงบันทึกเป็น exception จริง

### เหตุผลที่จัดลำดับความสำคัญไว้ท้ายสุด (จากที่คุยกันไว้)
งานนี้ซับซ้อนสุดในทุกข้อ (paradigm ใหม่ + exception system + UI แผนที่เฉพาะ) จึงตั้งใจทำหลังสุดตอนของอื่นเสร็จ/นิ่งแล้ว เพื่อลด risk ของ diff ใหญ่เกินตรวจสอบในรอบเดียว

---

## 💬 วิธีเริ่มแชทใหม่
บอก Claude ว่า:
> "อ่านไฟล์ `HANDOFF_SESSION_2026-07-10.md` ที่แนบมา แล้วเริ่มทำข้อ 5 ต่อ (โหมดที่ 4 + exception วันหยุด + หน้าตรวจสอบด้วยแผนที่)"

พร้อมแนบไฟล์ที่แก้ไขแล้วทั้งหมด (list ด้านบนหัวข้อ "ไฟล์ทั้งหมดที่แก้ไข") — **ไม่ใช่ไฟล์ต้นฉบับเดิมจาก repo**
